import 'package:flutter/material.dart';

class ProductTaxesView extends StatelessWidget {
  const ProductTaxesView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      child: Text('TAxes'),
    );
  }
}
